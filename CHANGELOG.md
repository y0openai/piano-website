# PIANO 웹사이트 변경 이력 (Changelog)

> 홈페이지 공개 콘텐츠·실적 수치의 주요 변경을 기록합니다.
> 비개발자도 "왜 이 숫자가 바뀌었는지" 나중에 바로 이해할 수 있도록 작성합니다.

---

## 2026-08-15 — 델타 거래 합산 + 수익률을 시간가중수익률(TWR)로 교체

### 왜 (배경)

두 가지 어긋남이 동시에 발견됐다.

**(1) 지표별 집계 범위 불일치.** 감마·델타 라흐마니노프는 **같은 바이낸스 계좌**다
(API 키·시크릿 SHA256 동일, 순자산 스냅샷이 같은 시각 같은 금액을 가리킴). 수익률은
계좌 순자산 기준이라 델타 매매 손익이 **이미 포함**되어 있었는데, 승률·거래수는
감마 폴더만 읽어 델타 거래 30건이 통째로 빠져 있었다. 같은 계좌·같은 기간인데
지표마다 분모가 달랐다. 홈페이지 문구도 "한 설정(감마)"이라 사실과 달랐다.

**(2) 입금이 수익률을 부풀림.** 기존 방식은
`(마지막 순자산 − 첫 순자산) ÷ 첫 순자산`이었다. 입금액 자체는 곡선에서 상쇄하지만
**그 입금액이 벌어온 수익은 곡선에 남는** 반면 분모는 최초 자본에 고정된다.
측정 기간 중 입출금이 3회, 순 +9,002 USDT — 초기 자본 8,556과 맞먹는 규모였다.

- 구간별 실측 (감마 순자산 스냅샷 17,931개)

  | 구간 | 순자산 변화 | 구간 수익률 | 직후 입출금 |
  | --- | --- | --- | --- |
  | 06-05 → 08-02 | 8,556 → 9,609 | +12.31% | +3,999 |
  | 08-02 → 08-03 | 13,609 → 13,611 | +0.02% | −1,999 |
  | 08-03 → 08-04 | 11,612 → 11,649 | +0.32% | +7,002 |
  | 08-04 → 08-15 | 18,651 → 18,760 | +0.59% | — |

  자본이 두 배가 된 8/4 이후 성과 +0.59%까지 초기 자본 8,556 기준으로 환산되고 있었다.

### 무엇을 (변경)

- **거래수·승률**: 감마 + 델타 합산. 두 봇의 심볼이 서로 겹치지 않아(감마 XAU·CL·NVDA 등 /
  델타 DOGE·XRP·ETH·GOOGL·SKHY) 중복 계상은 없다. 222건 → **252건**, 58.1% → **58.7%**
- **순자산**: 감마 폴더만 사용. 계좌 단위 값이라 두 폴더를 합치면 같은 돈을 두 번 센다.
- **수익률**: 시간가중수익률(TWR)로 교체. 입출금 지점에서 구간을 끊고 구간 수익률만
  곱해 이어붙여, 입금 이후 수익을 늘어난 자본 기준으로 평가한다. 15.78% → **13.41%**
- `piano-coin-gamma/scripts/publish_metrics.py`:
  `_load_equity_snapshots()`(원본 5분 스냅샷 로드) + `_time_weighted_return()` 신설.
  기존 `get_daily_equity()`는 입출금을 이미 상쇄한 일별 종가만 주어 "입출금이 언제
  있었나"를 알 수 없기 때문에 TWR에 쓸 수 없다. 발행 JSON에 `roiMethod: "TWR"` 추가.
  델타 폴더가 없으면 **발행 중단**(exit 1) — 생성자가 폴더를 자동 생성해서, 경로가
  틀려도 "델타 0건"으로 조용히 통과해 축소된 숫자를 올리기 때문.
- `js/i18n.js` · `index.html`: 문구를 "계좌 한 개(델타/감마 두 설정)"로,
  수익률 설명을 "입금·출금 영향을 걷어내고 계산한 시간가중수익률(TWR)"로 수정 (영/한).
  기존 "입출금 없음 기준"은 실제로 입금이 있었으므로 **틀린 진술**이었다.

### 발효 수치

2026-06-05 ~ 08-15 (71일): 수익률 **+13.41% (TWR)** · 승률 **58.7%** · 청산 거래 **252건**

> 측정 시작이 6월인 이유: 순자산 스냅샷 기록이 2026-06-05에 시작됐다. 거래 기록은
> 델타 4/3·감마 5/16부터 있지만(합 139건, +448 USDT), 시작 자본을 모르면 계좌
> 수익률을 계산할 수 없다. 제외된 구간이 이익이라 현재 표기는 보수적인 쪽이다.

### 컴플라이언스

`COMPLIANCE_GUIDELINE.md` §2 회색지대·§4 체크리스트 대조 완료. 실현 과거 실적 +
"미래 보장 없음/원금 손실 가능" 고지가 함께 표시된다. 이번 변경은 **표기를 사실에
맞추고 수익률을 더 낮게(보수적으로) 정정**하는 방향이라 리스크가 줄어든다.
문구 커밋과 숫자 커밋을 한 번의 push로 올려, 새 숫자가 옛 문구와 함께 노출되는
구간이 없도록 했다.

### 운영 메모

- `홈페이지-실적-갱신.bat`은 **수정 불필요**. 계산기가 델타 폴더를 추가로 읽을 뿐,
  실행 단계는 그대로다. (단, 한글 에러 메시지가 창에서 깨져 `chcp 65001` 한 줄을
  추가했다. 이 파일은 `.gitignore`의 `*.bat`에 걸려 **git에 올라가지 않으므로**
  다른 PC로 옮기면 다시 만들어야 한다.)
- 델타 저장소 경로(`c:\Users\DAL\Documents\piano-coin-delta`)에 의존한다.
  경로가 바뀌면 `publish_metrics.py`의 `DELTA_DATA_DIR`를 수정할 것.

### 커밋

- piano-website `133342c` fix(track): 실적 표기를 계좌 단위(델타/감마 합산)로 정정
- piano-website `af34988` chore(metrics): update track record (delta+gamma combined)
- piano-website `d68ecf7` fix(track): 수익률을 시간가중수익률(TWR)로 교체 (15.78% → 13.41%)
- piano-coin-gamma `d42b8c4` feat(metrics): 홈페이지 실적에 델타 라흐마니노프 거래 합산
- piano-coin-gamma `c16978a` fix(metrics): 계좌 수익률을 시간가중수익률(TWR)로 교체

---

## 2026-07-29 — 트랙레코드 수익률 정의 수정 (회전 마진 → 계좌 순자산)

### 왜 (배경)

홈페이지 트랙레코드의 "수익률"이 실제 계좌 성과보다 **약 10배 낮게** 표시되고
있었다. 라벨은 "투입자본 대비(on deployed capital)"였지만, 실제 발행 값은
`순손익 ÷ 청산된 모든 거래의 마진 누적합`이었다. 같은 자본이 수백 번 회전하며
분모에 중복 계산되어(회전율 희석), 실제 투자자가 계좌에서 겪은 수익률과
크게 어긋났다. 라벨과 계산식도 서로 달랐다.

- 검증 수치(라흐마니노프/gamma, 2026-06-05~07-29):
  - 순손익 835.64 USDT ÷ 마진 누적 17,468.71 USDT = **4.78%** (표시되던 값)
  - 계좌 순자산 8,429.04 → 9,374.31 USDT (입출금 없음, 순수 매매 성과) = **+11.22%**

### 무엇을 (변경)

수익률 정의를 **기간 중 계좌 순자산 변화율**로 교체. 세 지표(수익률·승률·거래수)를
모두 순자산 기록이 존재하는 구간(2026-06-05~)으로 통일해 기간을 일치시켰다.

- `js/metrics.js`: 발행 필드 `capitalRoiPct` → `accountRoiPct`
- `js/i18n.js`: 라벨 "수익률 (투입자본 대비)" → "수익률 (계좌 기준)"
  (영문 "Return (on deployed capital)" → "Return (account)"),
  disclaimer 문구를 "계좌 순자산 변화(입출금 없음 기준)"로 수정 (영/한)
- `index.html`: 정적 라벨 폴백 일치
- `piano-coin-gamma/scripts/publish_metrics.py`: 순자산 변화율로 `accountRoiPct`
  발행, 기간을 equity 구간으로 정렬, 승률·거래수 동일 구간 재계산

### 발효 수치

2026-06-05 ~ 07-29 (54일): 수익률 **+11.22%** · 승률 **59.0%** · 청산 거래 **156건**

### 컴플라이언스

`COMPLIANCE_GUIDELINE.md` 대조 완료. 실현 과거 실적 + "미래 보장 없음/원금 손실 가능"
고지가 함께 표시되어 §2 회색지대·§4 체크리스트상 허용. 계좌 기준 실현 수치는
가장 보수적·정직한 표현이라 컴플라이언스상 안전하다.

### 운영 메모

`홈페이지-실적-갱신.bat`은 `data/metrics.json`만 커밋/푸시하므로, 이번 라벨·JS
변경은 별도 1회 커밋으로 반영했다. 이후 bat은 새 `accountRoiPct` 필드와 호환되어
정상 동작한다.

### 커밋

- piano-website `08bc824` fix(track): publish account-equity return instead of turnover-diluted margin ROI
- piano-coin-gamma `801b008` fix(metrics): publish account-equity return, not turnover-diluted margin ROI
